# TriPrize Environment Variables Template
# 复制此文件为 .env 并填入真实的配置值

# Database Configuration
DB_PASSWORD=your_secure_password_here

# Firebase Configuration
# 从 Firebase Console 获取: https://console.firebase.google.com/
# Project Settings > Service Accounts > Generate New Private Key
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour Private Key Here\n-----END PRIVATE KEY-----\n"

# Stripe Configuration
# 从 Stripe Dashboard 获取: https://dashboard.stripe.com/test/apikeys
# 测试环境使用 sk_test_ 和 pk_test_ 开头的密钥
# 生产环境使用 sk_live_ 和 pk_live_ 开头的密钥
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# AWS S3 Configuration (可选 - 目前未使用)
# 图片存储在本地文件系统，不需要 AWS S3
# 如果将来需要使用云存储，可以配置这些值
# 从 AWS Console 获取: https://console.aws.amazon.com/iam/
# IAM > Users > Security Credentials > Create Access Key
AWS_REGION=ap-northeast-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_BUCKET=

# JWT Secret
# 生成一个至少32字符的随机字符串
# 可以使用: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_at_least_32_characters_long

# CORS Configuration
CORS_ORIGIN=http://localhost:8080

