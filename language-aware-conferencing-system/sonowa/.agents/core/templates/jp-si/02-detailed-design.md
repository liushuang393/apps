# <対象> 詳細設計書

- 作成日:
- 基本設計書:
- 対象リビジョン:

## 1. 画面設計

UI が無い場合は「UI 無し」と明記して本節を終える。空欄にしない。

### 1.1 画面一覧

| 画面ID | 画面名 | 用途 | 権限 |
| ------ | ------ | ---- | ---- |

### 1.2 画面遷移

```mermaid
stateDiagram-v2
```

### 1.3 画面項目定義

| 画面ID | 項目 | 型  | 必須 | 検証 | 初期値 |
| ------ | ---- | --- | ---- | ---- | ------ |

### 1.4 主要イベント

## 2. 異常時の統一出力設計

### 2.1 エラー分類

| 分類       | 例  | HTTP | 利用者への表示 | ログ出力 |
| ---------- | --- | ---- | -------------- | -------- |
| 業務エラー |     |      |                |          |
| 認証・認可 |     |      |                |          |
| 入力検証   |     |      |                |          |
| システム   |     |      |                |          |

### 2.2 統一レスポンス形式

```json
{}
```

### 2.3 失敗時の既定動作

> 判定不能時に許可へ倒れないこと。未実装経路は明示的な失敗とする。
> 例外メッセージをそのまま利用者へ返さない（利用者には固定文言、詳細はログへ）。

## 3. API 設計

| #   | メソッド | パス | 入力 | 出力 | 認可 | エラーコード |
| --- | -------- | ---- | ---- | ---- | ---- | ------------ |

## 4. インターフェース / 契約設計

| 契約 | 提供側 | 利用側 | 破壊的変更の可否 | 互換維持方法 |
| ---- | ------ | ------ | ---------------- | ------------ |

## 5. データ設計

### 5.1 定義

| エンティティ | 属性 | 型  | 制約 | 索引 |
| ------------ | ---- | --- | ---- | ---- |

### 5.2 移行

データ形状が変わる場合のみ。変わらない場合は「移行なし」と明記。

| 項目           | 内容 |
| -------------- | ---- |
| 前進手順       |      |
| 併存期間       |      |
| 読み手・書き手 |      |
| 巻き戻し手順   |      |
| 検証方法       |      |

## 6. タスク分解表

受入条件 1 つ = タスク 1 件。

| タスクID | 受入条件ID | 依存 | 並行 | 対象ファイル | 完了条件 |
| -------- | ---------- | ---- | ---- | ------------ | -------- |

> `並行` は依存構造の記録であり、並列実行の許可ではない。書き込みは常に単一。

## 7. 単体テストケース一覧

| タスクID | 受入条件ID | 区分               | ケース名 | 入力 | 期待結果 |
| -------- | ---------- | ------------------ | -------- | ---- | -------- |
|          |            | 正常 / 異常 / 境界 |          |      |          |

> ここに書いたケースが、実装前に失敗させる（RED）対象になる。

## 8. 制約条件・確認事項

## 9. 改訂履歴

| 日付 | 版  | 変更内容 | 変更したタスク |
| ---- | --- | -------- | -------------- |
